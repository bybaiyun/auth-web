<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dal.mapper.StudentMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.dal.domain.Student">
            <result property="sid" column="sid" jdbcType="VARCHAR"/>
            <result property="sName" column="sname" jdbcType="VARCHAR"/>
            <result property="sAge" column="sage" jdbcType="TIMESTAMP"/>
            <result property="sSex" column="ssex" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        sid,sname,sage,
        ssex
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from student
        where 
    </select>

    <select id="selectAllStudent" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />
        from student
        where 1 = 1
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from student
        where 
    </delete>
    <insert id="insert">
        insert into student
        ( sid,sname,sage
        ,ssex)
        values (#{sid,jdbcType=VARCHAR},#{sname,jdbcType=VARCHAR},#{sage,jdbcType=TIMESTAMP}
        ,#{ssex,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective">
        insert into student
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="sid != null">sid,</if>
                <if test="sname != null">sname,</if>
                <if test="sage != null">sage,</if>
                <if test="ssex != null">ssex,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="sid != null">#{sid,jdbcType=VARCHAR},</if>
                <if test="sname != null">#{sname,jdbcType=VARCHAR},</if>
                <if test="sage != null">#{sage,jdbcType=TIMESTAMP},</if>
                <if test="ssex != null">#{ssex,jdbcType=VARCHAR},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.example.demo.dal.domain.Student">
        update student
        <set>
                <if test="sid != null">
                    sid = #{sid,jdbcType=VARCHAR},
                </if>
                <if test="sname != null">
                    sname = #{sname,jdbcType=VARCHAR},
                </if>
                <if test="sage != null">
                    sage = #{sage,jdbcType=TIMESTAMP},
                </if>
                <if test="ssex != null">
                    ssex = #{ssex,jdbcType=VARCHAR},
                </if>
        </set>
        where  
    </update>
    <update id="updateByPrimaryKey" parameterType="com.example.demo.dal.domain.Student">
        update student
        set 
            sid =  #{sid,jdbcType=VARCHAR},
            sname =  #{sname,jdbcType=VARCHAR},
            sage =  #{sage,jdbcType=TIMESTAMP},
            ssex =  #{ssex,jdbcType=VARCHAR}
        where  
    </update>
</mapper>
